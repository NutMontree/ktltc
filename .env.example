# คัดลอกเป็น .env.local (แนะนำ) หรือเพิ่มใน .env แล้วใส่ค่าจริง
# หลังแก้ไขต้อง restart: npm run dev

# --- ระบบหลัก (มีอยู่แล้วในโปรเจกต์) ---
# MONGODB_URI=
# AUTH_SECRET=

# --- DVE อ่านคะแนนจากรูป ---
# ใช้ Tesseract OCR ฟรีในเครื่อง/เบราว์เซอร์ — ไม่ต้องตั้งค่าเพิ่ม ไม่เสียเงิน
