import FullPageLoader from "@/components/FullPageLoader";

export default function AdminLoading() {
  return <FullPageLoader message="กำลังเปลี่ยนหน้า..." subtitle="ระบบกำลังเตรียมข้อมูลหน้าถัดไปให้คุณ" />;
}
